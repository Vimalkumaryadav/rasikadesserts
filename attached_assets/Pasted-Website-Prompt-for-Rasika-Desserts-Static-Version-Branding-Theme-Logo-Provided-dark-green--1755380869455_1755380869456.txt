Website Prompt for Rasika Desserts (Static Version)
🎨 Branding & Theme

Logo: Provided (dark green background, golden “Rasika” in royal style).

Colors:

Dark Green (#013220) → Primary

Gold (#FFD700) → Accent

Cream (#FFF8E1) → Highlights

White (#FFFFFF) → Text contrast

Fonts:

Headings → Playfair Display (serif, luxury)

Body text → Montserrat (sans-serif, clean)

Style: Luxurious, elegant, minimal, appetizing visuals.

🌟 Website Sections (Single Page)
1. Hero Section

Background: Dark green with subtle gold gradient overlay.

Centered logo.

Tagline: “Where Tradition Meets Royal Indulgence”.

CTA Buttons:

“Order on Swiggy”

“Order on Zomato”
(open respective app on mobile, fallback to web on desktop).

2. Product Showcase

Grid layout with responsive product cards.

Each card includes:

Product image (high-quality close-up).

Product name (e.g., Apricot Delight).

Short description (1–2 lines).

Price (e.g., “₹250 / box”).

Discount badge (if applicable, e.g., “20% OFF”).

Future-proof: Easily add more products without breaking layout.

3. Party Orders

Section title: “Planning a Celebration? We Cater Too!”

Description: Short, inviting text → “Order in bulk for weddings, birthdays, and special occasions.”

Button: “Order via WhatsApp”

Redirects to WhatsApp Business link or WhatsApp Group link.

Example: https://wa.me/<business_number>?text=Hi%20Rasika%20Desserts,%20I%20want%20to%20place%20a%20party%20order.

4. Footer

Background: Dark green, gold typography.

Contents:

Contact info (phone & email).

Social media icons (Instagram, Facebook).

Copyright © Rasika Desserts 2025.

⚙️ Deployment (GitHub Pages + Actions)

Since no backend is required, this will be a static site.

Built with React + Vite (recommended for maintainability).

Deployed via GitHub Pages using GitHub Actions.

GitHub Actions Workflow
name: Deploy Website to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v3

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2

✅ Final Version

Single page, luxury design.

Products displayed in a grid with images + prices.

Party orders handled via WhatsApp redirect (no backend needed).

Hosted on GitHub Pages with automatic deployment.